# Chapter 9: 设计网络爬虫 (Design a Web Crawler)

> 📑 **导航**:[← 短链](ch1_08-设计短链服务.md) · [📚 总目录](../README.md) · [通知系统 →](ch1_10-设计通知系统.md)
> 🔗 **相关**:[搜索补全](ch1_13-设计搜索自动补全.md) · [AWS存储](../SDA/aws_10-AWS存储服务.md) · [AWS计算](../SDA/aws_11-AWS计算服务.md) · [AWS消息/IAM](../SDA/aws_12-AWS消息编排监控IAM.md)


> **本章定位**:爬虫是面试**最复杂的"构件组合题"之一**——它把 Ch1 的 MQ、Ch6 的去重、Ch7 的 ID、BFS、布隆过滤器、DNS、容错全用上。Google/Bing 的核心就是爬虫。掌握这道题,系统设计能力上一个台阶。

> **本章和原书的区别**:原书讲了 frontier/去重/DNS,但**反爬(JS 渲染、验证码、IP 轮换)完全没提**——这是 2026 爬虫的真问题。本章补 **Headless 浏览器(Playwright)、反爬策略、分布式架构**,因为现代网页大量 JS 渲染,传统 HTTP 抓取抓不到内容。

---

## 🎯 面试怎么答(被问到"设计一个网络爬虫"时)

**开场话术**(直接背):

> "爬虫核心循环:**取 URL → 下载 → 解析 → 提取新 URL → 去重 → 入队**。我先定规模和目标(网页数、抓取频率),然后讲五个关键点:**① BFS frontier ② 去重(布隆) ③ DNS 缓存 ④ politeness ⑤ 容错**。最后补 2026 的反爬(JS 渲染)。"

**5 步推进**:

```mermaid
flowchart LR
    S1["① BFS frontier<br/>(优先级队列)"] --> S2["② 去重<br/>(布隆过滤器)"]
    S2 --> S3["③ DNS 缓存<br/>(性能)"]
    S3 --> S4["④ politeness<br/>(robots.txt + 延迟)"]
    S4 --> S5["⑤ 容错<br/>(超时/黑洞/trap)"]

    style S1 fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style S2 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style S3 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style S4 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style S5 fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
```

---

## 🗺️ 章节概览

| 小节 | 要点 |
|------|------|
| 需求 + 估算 | 10亿页面、月级抓取、新内容延迟 |
| 高层架构 | 取→下→解析→存→去重→入队 |
| **BFS + frontier** | 优先级队列 + politeness 队列 |
| **去重** | 布隆过滤器(内存省) |
| **DNS 缓存** | DNS 是性能瓶颈 |
| politeness | robots.txt + 同站点延迟 |
| 优先级 + fresh | 重要页面优先 + 重新抓取 |
| 容错 | 超时、黑洞、爬虫 trap、循环 |
| **反爬(2026)** | JS 渲染、验证码、IP 轮换 |

---

## 1. 需求 + 估算

**澄清问题**(Step 1):
- 抓什么?(HTML / 图片 / 全部)
- 规模?(10 亿页面 / 100 亿)
- 新内容多快发现?(天 / 周 / 月)
- 是否遵守 robots.txt?(必须)

**估算**(假设 10 亿页面,4 周抓完):
```
QPS = 10亿 / (4周 × 7 × 86400) ≈ 410 页/秒
存储:每页 ~500KB(含图片)→ 10亿 × 500KB = 500 PB
URL 去重表:10亿 URL × 100B = 100 GB
```

> 💡 **结论**:爬虫是**高吞吐 + 大存储**系统。重点是**下载吞吐**和**去重内存**。

---

## 2. 高层架构

```mermaid
flowchart LR
    SEED["Seed URLs"] --> F["URL Frontier<br/>(优先级队列)"]
    F --> DQ["下载器<br/>(Downloader)"]
    DQ -->|"DNS 缓存"| DNS["DNS"]
    DQ --> P["解析器<br/>(HTML 解析)"]
    P --> STORE[("存储<br/>对象存储 + KV")]
    P --> NEW["新 URL"]
    NEW --> DUP{"去重?<br/>(布隆过滤器)"}
    DUP -->|"新"| F
    DUP -->|"已抓"| SKIP["丢弃"]

    style SEED fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style F fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style DQ fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style DNS fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style P fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style STORE fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style NEW fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style DUP fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style SKIP fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
```

**核心循环**(背熟):**Seed → Frontier → 下载 → 解析 → 提取新 URL → 去重 → 入 Frontier**。这是爬虫的心脏。

**单 URL 处理时序**(画出这张图,面试官知道你懂细节):

```mermaid
sequenceDiagram
    participant F as Frontier
    participant W as Worker
    participant BF as 布隆过滤器
    participant DNS as DNS缓存
    participant S as 目标站点

    F->>W: pop(url)
    W->>BF: 已抓过?
    BF-->>W: 否(新URL)
    W->>W: robots.txt 检查
    W->>W: politeness 等待(同站延迟)
    W->>DNS: 查 host IP
    alt 缓存命中
        DNS-->>W: IP
    else 未命中
        DNS-->>W: 解析(慢)
    end
    W->>S: GET url (超时10s)
    S-->>W: HTML
    W->>W: 防黑洞(size<10MB)
    W->>W: 解析 + SimHash 近重复检测
    W->>F: push(新URLs)
    Note over W,BF: 新URL入队前再次查布隆
```

---

## 3. BFS + URL Frontier ⭐⭐

### 为什么用 BFS 不用 DFS

```mermaid
flowchart LR
    BFS["✅ BFS<br/>────────<br/>• 广度优先<br/>• 先抓浅层(重要页面)<br/>• 不会陷入深层死循环"] ~~~ DFS["❌ DFS<br/>────────<br/>• 深度优先<br/>• 可能陷入 trap 无限深<br/>• 漏掉浅层重要页面"]

    style BFS fill:#E8F5E9,stroke:#388E3C,color:#1f1f1f
    style DFS fill:#FFEBEE,stroke:#C62828,color:#1f1f1f
```

### URL Frontier(优先级 + politeness 双队列)⭐

这是爬虫的核心数据结构,不是简单 FIFO。它要解决两个问题:

```mermaid
flowchart TD
    F["URL Frontier"] --> PQ["① 优先级队列<br/>(重要页面优先)"]
    F --> MQ["② Politeness 队列<br/>(同站点不并发)"]

    style F fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style PQ fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style MQ fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

| 队列 | 解决的问题 | 做法 |
|------|----------|------|
| **优先级队列** | 重要页面先抓(如首页 vs 深层页) | 按 PageRank/权重排序 |
| **Politeness 队列** | **同一站点不并发**(否则被封) | 每个站点一个 FIFO 队列,轮询出队,加延迟 |

> 📝 **Politeness**:抓取同一站点的两个 URL 之间**必须有延迟**(如 1 秒),否则会被站点当成 DDoS 封 IP。这是爬虫礼仪。

> 🪤 **追问陷阱**:"为什么 frontier 不是单个 FIFO?" → "单个 FIFO 会**连续抓同一站点**(FIFO 顺序可能集中),触发反爬被封。**politeness 队列**让不同站点轮流出队,保证同站点延迟。"

---

# 【🌹自己笔记】这两个队列怎么工作的
<img width="1536" height="2225" alt="image" src="https://github.com/user-attachments/assets/9dd86027-07cd-4292-9bfe-a13bd6056c31" />

优先级队列的作用： 哪怕大量低价值内页最先被爬虫解析出来，高权重首页依旧会被排到最前面，优先分发抓取。
Politeness队列： 保证网站不会被连续爬

假如仅单一FIFO队列
原始发现顺序：A详情3→A详情2→C深层页→A首页→B首页→A栏目页→B详情页→C栏目页
抓取顺序完全跟随发现顺序：
先连续抓取3个低价值页面，再抓首页；且前3条全是A站页面，**瞬间连续猛攻A站，直接触发IP封禁**

```
# 优化具象案例：完整凸显优先级队列的筛选作用
## 基础设定
1. 权重规则
首页权重=10，栏目页权重=5，深层详情页权重=1。数值越高，优先级越高。
2. 礼貌队列：A、B、C站点各自独立FIFO队列，同站点抓取强制冷却1s。
3. 链路：抓取发现新URL → 全局优先级大堆排序 → 分发至站点礼貌队列 → 轮询延时抓取。
4. 本次一次性抓取解析得到8条URL：
A详情3(1)、A详情2(1)、C深层页(1)、A首页(10)、B首页(10)、A栏目页(5)、B详情页(1)、C栏目页(5)

## 阶段1：全部URL进入优先级队列，重新降序排序
原始杂乱列表会被按照权重重新排队，队列靠前=优先取出：
1.A首页(10)、2.B首页(10)、3.A栏目页(5)、4.C栏目页(5)、5.A详情2(1)、6.A详情3(1)、7.B详情页(1)、8.C深层页(1)
> 关键作用：
哪怕大量低价值内页最先被爬虫解析出来，**高权重首页依旧会被排到最前面，优先分发抓取**；
单纯FIFO只会“谁先发现谁先抓”，会先抓一堆没用的底层页面，浪费算力。

## 阶段2：按顺序逐条取出，分发到对应站点礼貌队列
1. A首页 → A队列
2. B首页 → B队列
3. A栏目页 → A队列
4. C栏目页 → C队列
5. A详情2 → A队列
6. A详情3 → A队列
7. B详情页 → B队列
8. C深层页 → C队列

最终各站点队列：
A队列：[A首页，A栏目页，A详情2，A详情3]
B队列：[B首页，B详情页]
C队列：[C栏目页，C深层页]

## 阶段3：礼貌队列轮询抓取（控速防封禁，优先级只决定入队顺序）
规则：依次轮询A→B→C，每个站点一轮最多取1条；同站点抓取后冷却1秒。
### 第一轮轮询
A冷却放行：抓取A首页，A冷却计时+1s
B冷却放行：抓取B首页，B冷却计时+1s
C冷却放行：抓取C栏目页，C冷却计时+1s
剩余队列：
A：[A栏目页、A详情2、A详情3]
B：[B详情页]
C：[C深层页]

### 立刻第二轮轮询
A、B、C均未度过1秒冷却，全部跳过，等待。

### 1秒后 第三轮轮询
A冷却结束：抓取A栏目页，重新冷却1s
B冷却结束：抓取B详情页，重新冷却1s
C冷却结束：抓取C深层页，重新冷却1s
剩余队列：A：[A详情2、A详情3]

### 等待1s，第四轮轮询
A抓取A详情2，冷却重置
### 等待1s，第五轮轮询
A抓取A详情3，抓取完毕。

## 核心对比，一眼看懂优先级价值
### 场景1：Frontier（优先级+礼貌队列）
抓取顺序：A首页→B首页→C栏目页→A栏目页→B详情页→C深层页→A详情2→A详情3
**高价值页面全部优先抓取**，爬虫最快拿到网站核心内容。

### 场景2：仅单一FIFO队列
原始发现顺序：A详情3→A详情2→C深层页→A首页→B首页→A栏目页→B详情页→C栏目页
抓取顺序完全跟随发现顺序：
先连续抓取3个低价值页面，再抓首页；且前3条全是A站页面，瞬间连续猛攻A站，直接触发IP封禁。

## 分工总结
1. **优先级队列**：全局筛选，决定「哪些链接更早进入站点队列」，保证核心高价值页面优先排队；
2. **多站点礼貌队列**：控制抓取节奏，决定「什么时候抓取某条链接」，打散同站点请求、防反爬封禁；
3. 二者缺一不可：优先级只管先后次序，礼貌队列只管速度与间隔。
```


## 4. URL 去重:布隆过滤器 ⭐⭐

10 亿 URL,去重表 100GB?太大了。用**布隆过滤器**(Bit array + 多哈希):

```mermaid
flowchart LR
    URL["URL"] --> H1["哈希1"]
    URL --> H2["哈希2"]
    URL --> H3["哈希3"]
    H1 --> BF["位数组<br/>(1GB)"]
    H2 --> BF
    H3 --> BF
    BF --> Q{"所有位都=1?"}
    Q -->|"是"| MAYBE["可能已抓(误判)"]
    Q -->|"否"| NEW["肯定新 → 抓"]

    style URL fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style H1 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style H2 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style H3 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style BF fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style Q fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style MAYBE fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style NEW fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
```

| 特性 | 说明 |
|------|------|
| **说"不在"** | ✅ **肯定不在**(无假阴性) |
| **说"在"** | ⚠️ **可能在**(有假阳性,可接受) |
| **内存** | 10亿 URL 只需 ~1-2 GB(对比哈希表 100GB) |

> 💡 **为什么用布隆**:爬虫 URL 量巨大,哈希表存不下。布隆过滤器**省内存 100 倍**,假阳性(少量重复抓)可接受。

> 🪤 **追问陷阱**:"布隆过滤器的误判怎么处理?" → "误判(说已抓,实际没)会漏抓少量页面。解法:① 重要页面用 KV 表精确去重;② 定期重建布隆;③ 接受少量漏抓。"

> 🪤 **追问陷阱**:"重抓(fresh)时,布隆里已经有的 URL 怎么办?" → "标准布隆**不能删除**(删一个位会影响多个元素)。重抓需求用 **Counting Bloom Filter**(位数组换成计数器,可增可减),或**周期性重建布隆**(配合 fresh 周期)。"

---

# 【🌹自己笔记】 什么是重抓，怎么支持
```

# 一、通俗定义：什么是爬虫的「重抓（fresh 刷新抓取）」
## 1. 基础静态爬虫（一次性抓取）
普通一次性爬虫：一个URL抓过1次，标记为已抓取，**永久不再抓取**。
适用场景：静态网页、不会改动的文章、归档页面。

## 2. 重抓/刷新抓取（fresh crawl）
绝大多数网站内容会实时更新：新闻、论坛帖子、商品价格、动态首页。
因此爬虫需要**隔一段时间，重新抓取已经抓过的旧URL**，查看内容是否更新，这个行为就叫**重抓**。
举例子：
- 今日头条首页每5分钟更新热点；
- 电商商品价格全天浮动；
爬虫不能只抓1次，必须定时回头重复抓取老链接。

## 3. 标准布隆过滤器和重抓的核心矛盾
1. 普通布隆过滤器：**只支持插入标记，不支持删除**
原理：多个不同URL会哈希映射到同一个二进制bit位。
假如A、B两个URL都占用bit5，你想删除A的标记，把bit5置0，B也会被误标记为“未抓取”。
2. 矛盾：
重抓需要**临时抹除旧URL的“已抓取标记”**，让系统允许再次抓取；
普通布隆无法删除标记，一旦URL入库，永远判定为已抓取，无法重抓刷新。

# 二、行业主流3套解决方案（从简单到工程落地）
## 方案1：周期性全量重建布隆过滤器（最简单，中小型爬虫首选）
### 原理
不修改布隆结构，靠时间周期解决刷新需求：
1. 设定刷新周期，例如7天；
2. 布隆过滤器只在当前周期生效，记录**最近7天抓取过的URL**；
3. 7天一到，直接丢弃旧布隆，新建一个空布隆；
4. 所有历史URL全部恢复为“未抓取状态”，爬虫可以全盘重抓一轮。

### 优缺点
✅ 优点：实现最简单、无额外内存开销、架构不变；
❌ 缺点：周期内无法灵活单独刷新某一个页面，只能一刀切全局刷新。
适用：通用全网爬虫、时效性不强的网页。

## 方案2：计数布隆过滤器 Counting Bloom Filter（精准删除，大型爬虫常用）
### 改造细节
把原始布隆的**二进制bit数组（只有0和1）**，替换为**计数器数组**，一般用4bit做一个计数器：
1. 插入URL：对应N个哈希位置的计数器+1；
2. 判断存在：任意一个哈希位置计数器=0 → 一定不存在；全部＞0 → 大概率存在；
3. 删除URL：对应N个哈希位置的计数器-1。

### 解决重抓逻辑
需要重抓某个URL时，直接对该URL执行**删除操作**，布隆就会认为该链接未抓取，可重新抓取。

### 优缺点
✅ 优点：支持精准单独删除、定点刷新任意URL，灵活性极高；
❌ 缺点：内存放大4~8倍；依然存在假阳性，无假阴性。
适用：新闻、短视频、实时资讯这类需要随时定点刷新的爬虫。

## 方案3：分层混合架构（工业级成熟方案，大厂爬虫标配）
### 架构拆分
1. **布隆过滤器（高速内存层）**：负责海量普通URL快速粗略去重；
2. **KV精确数据库（Redis/磁盘KV）**：存放高频、高重要性、需要频繁重抓的核心URL；

### 运行逻辑
1. 普通海量网页：只用布隆过滤，周期重建实现批量重抓；
2. 首页、热点、核心帖子、商品：存入KV做精准标记，想重抓直接手动修改KV状态，完全不受布隆限制；
3. 布隆只过滤海量低频页面，KV管控少量高频刷新页面。

### 优缺点
✅ 兼顾内存占用 + 灵活定点重抓，性能与可控性平衡最好；
❌ 架构略微复杂。

## 方案4：过期时间布隆（时间轮布隆，轻量化定时刷新）
给每一组bit绑定过期时间，URL标记后自动倒计时，到期自动失效，到期后自动允许重抓。
适合固定间隔统一刷新的场景。

# 三、整理面试标准答案
## 问题1：重抓是什么？
网页内容会持续更新，爬虫需要对已经抓取过的URL二次、多次抓取，校验内容更新，该行为就是重抓。

## 问题2：普通布隆为什么无法满足重抓？
标准布隆过滤器仅支持插入，不支持删除；多个URL哈希共享bit位，删除单个元素会污染其他URL标记，因此无法单独放开某URL的抓取限制。

## 问题3：3种标准解决思路
1. **周期重建**：定期废弃旧布隆、新建空过滤器，全局统一放开重抓，实现最简单；
2. **计数布隆过滤器**：bit改为计数器，支持单独删除标记，定点随时重抓，内存开销提升；
3. **分层架构**：海量URL用布隆粗略去重，高频重点URL用KV精确管理，灵活手动控制重抓。

# 补充：前文提到的布隆误判（假阳性）回顾
假阳性：过滤器判定URL已抓取，实际从未抓取，造成漏抓。
解决：
1. 高价值页面落地KV精确去重；
2. 合理调高哈希函数数量，降低误判概率；
3. 接受极少量漏抓，互联网爬虫可以容忍轻微漏抓。
```

# 【🌹自己笔记】 要实现重抓怎么办
```
# 一、通俗定义：什么是爬虫的「重抓（fresh 刷新抓取）」
## 1. 基础静态爬虫（一次性抓取）
普通一次性爬虫：一个URL抓过1次，标记为已抓取，**永久不再抓取**。
适用场景：静态网页、不会改动的文章、归档页面。

## 2. 重抓/刷新抓取（fresh crawl）
绝大多数网站内容会实时更新：新闻、论坛帖子、商品价格、动态首页。
因此爬虫需要**隔一段时间，重新抓取已经抓过的旧URL**，查看内容是否更新，这个行为就叫**重抓**。
举例子：
- 今日头条首页每5分钟更新热点；
- 电商商品价格全天浮动；
爬虫不能只抓1次，必须定时回头重复抓取老链接。

## 3. 标准布隆过滤器和重抓的核心矛盾
1. 普通布隆过滤器：**只支持插入标记，不支持删除**
原理：多个不同URL会哈希映射到同一个二进制bit位。
假如A、B两个URL都占用bit5，你想删除A的标记，把bit5置0，B也会被误标记为“未抓取”。
2. 矛盾：
重抓需要**临时抹除旧URL的“已抓取标记”**，让系统允许再次抓取；
普通布隆无法删除标记，一旦URL入库，永远判定为已抓取，无法重抓刷新。

# 二、行业主流3套解决方案（从简单到工程落地）
## 方案1：周期性全量重建布隆过滤器（最简单，中小型爬虫首选）
### 原理
不修改布隆结构，靠时间周期解决刷新需求：
1. 设定刷新周期，例如7天；
2. 布隆过滤器只在当前周期生效，记录**最近7天抓取过的URL**；
3. 7天一到，直接丢弃旧布隆，新建一个空布隆；
4. 所有历史URL全部恢复为“未抓取状态”，爬虫可以全盘重抓一轮。

### 优缺点
✅ 优点：实现最简单、无额外内存开销、架构不变；
❌ 缺点：周期内无法灵活单独刷新某一个页面，只能一刀切全局刷新。
适用：通用全网爬虫、时效性不强的网页。

## 方案2：计数布隆过滤器 Counting Bloom Filter（精准删除，大型爬虫常用）
### 改造细节
把原始布隆的**二进制bit数组（只有0和1）**，替换为**计数器数组**，一般用4bit做一个计数器：
1. 插入URL：对应N个哈希位置的计数器+1；
2. 判断存在：任意一个哈希位置计数器=0 → 一定不存在；全部＞0 → 大概率存在；
3. 删除URL：对应N个哈希位置的计数器-1。

### 解决重抓逻辑
需要重抓某个URL时，直接对该URL执行**删除操作**，布隆就会认为该链接未抓取，可重新抓取。

### 优缺点
✅ 优点：支持精准单独删除、定点刷新任意URL，灵活性极高；
❌ 缺点：内存放大4~8倍；依然存在假阳性，无假阴性。
适用：新闻、短视频、实时资讯这类需要随时定点刷新的爬虫。

## 方案3：分层混合架构（工业级成熟方案，大厂爬虫标配）
### 架构拆分
1. **布隆过滤器（高速内存层）**：负责海量普通URL快速粗略去重；
2. **KV精确数据库（Redis/磁盘KV）**：存放高频、高重要性、需要频繁重抓的核心URL；

### 运行逻辑
1. 普通海量网页：只用布隆过滤，周期重建实现批量重抓；
2. 首页、热点、核心帖子、商品：存入KV做精准标记，想重抓直接手动修改KV状态，完全不受布隆限制；
3. 布隆只过滤海量低频页面，KV管控少量高频刷新页面。

### 优缺点
✅ 兼顾内存占用 + 灵活定点重抓，性能与可控性平衡最好；
❌ 架构略微复杂。

## 方案4：过期时间布隆（时间轮布隆，轻量化定时刷新）
给每一组bit绑定过期时间，URL标记后自动倒计时，到期自动失效，到期后自动允许重抓。
适合固定间隔统一刷新的场景。

# 三、整理面试标准答案
## 问题1：重抓是什么？
网页内容会持续更新，爬虫需要对已经抓取过的URL二次、多次抓取，校验内容更新，该行为就是重抓。

## 问题2：普通布隆为什么无法满足重抓？
标准布隆过滤器仅支持插入，不支持删除；多个URL哈希共享bit位，删除单个元素会污染其他URL标记，因此无法单独放开某URL的抓取限制。

## 问题3：3种标准解决思路
1. **周期重建**：定期废弃旧布隆、新建空过滤器，全局统一放开重抓，实现最简单；
2. **计数布隆过滤器**：bit改为计数器，支持单独删除标记，定点随时重抓，内存开销提升；
3. **分层架构**：海量URL用布隆粗略去重，高频重点URL用KV精确管理，灵活手动控制重抓。

# 补充：前文提到的布隆误判（假阳性）回顾
假阳性：过滤器判定URL已抓取，实际从未抓取，造成漏抓。
解决：
1. 高价值页面落地KV精确去重；
2. 合理调高哈希函数数量，降低误判概率；
3. 接受极少量漏抓，互联网爬虫可以容忍轻微漏抓。
```

# 【🌹自己笔记】如果每个bit位都被占住了，即使用counting bloom filter, 不还是重抓不了吗？

问题：比如有 10 个 bit 位，正好每个 bit 位都被不同的 url 占了，然后再来了一个 url, 他就算每个 bit 位都 + 1，然后删除，还是被重抓不了啊
Bloom filter设计的时候空间要足够大，不会让每个bit位都占那么满

```
# 一、通俗定义：什么是爬虫的「重抓（fresh 刷新抓取）」
## 1. 基础静态爬虫（一次性抓取）
普通一次性爬虫：一个URL抓过1次，标记为已抓取，**永久不再抓取**。
适用场景：静态网页、不会改动的文章、归档页面。

## 2. 重抓/刷新抓取（fresh crawl）
绝大多数网站内容会实时更新：新闻、论坛帖子、商品价格、动态首页。
因此爬虫需要**隔一段时间，重新抓取已经抓过的旧URL**，查看内容是否更新，这个行为就叫**重抓**。
举例子：
- 今日头条首页每5分钟更新热点；
- 电商商品价格全天浮动；
爬虫不能只抓1次，必须定时回头重复抓取老链接。

## 3. 标准布隆过滤器和重抓的核心矛盾
1. 普通布隆过滤器：**只支持插入标记，不支持删除**
原理：多个不同URL会哈希映射到同一个二进制bit位。
假如A、B两个URL都占用bit5，你想删除A的标记，把bit5置0，B也会被误标记为“未抓取”。
2. 矛盾：
重抓需要**临时抹除旧URL的“已抓取标记”**，让系统允许再次抓取；
普通布隆无法删除标记，一旦URL入库，永远判定为已抓取，无法重抓刷新。

# 二、行业主流3套解决方案（从简单到工程落地）
## 方案1：周期性全量重建布隆过滤器（最简单，中小型爬虫首选）
### 原理
不修改布隆结构，靠时间周期解决刷新需求：
1. 设定刷新周期，例如7天；
2. 布隆过滤器只在当前周期生效，记录**最近7天抓取过的URL**；
3. 7天一到，直接丢弃旧布隆，新建一个空布隆；
4. 所有历史URL全部恢复为“未抓取状态”，爬虫可以全盘重抓一轮。

### 优缺点
✅ 优点：实现最简单、无额外内存开销、架构不变；
❌ 缺点：周期内无法灵活单独刷新某一个页面，只能一刀切全局刷新。
适用：通用全网爬虫、时效性不强的网页。

## 方案2：计数布隆过滤器 Counting Bloom Filter（精准删除，大型爬虫常用）
### 改造细节
把原始布隆的**二进制bit数组（只有0和1）**，替换为**计数器数组**，一般用4bit做一个计数器：
1. 插入URL：对应N个哈希位置的计数器+1；
2. 判断存在：任意一个哈希位置计数器=0 → 一定不存在；全部＞0 → 大概率存在；
3. 删除URL：对应N个哈希位置的计数器-1。

### 解决重抓逻辑
需要重抓某个URL时，直接对该URL执行**删除操作**，布隆就会认为该链接未抓取，可重新抓取。

### 优缺点
✅ 优点：支持精准单独删除、定点刷新任意URL，灵活性极高；
❌ 缺点：内存放大4~8倍；依然存在假阳性，无假阴性。
适用：新闻、短视频、实时资讯这类需要随时定点刷新的爬虫。

## 方案3：分层混合架构（工业级成熟方案，大厂爬虫标配）
### 架构拆分
1. **布隆过滤器（高速内存层）**：负责海量普通URL快速粗略去重；
2. **KV精确数据库（Redis/磁盘KV）**：存放高频、高重要性、需要频繁重抓的核心URL；

### 运行逻辑
1. 普通海量网页：只用布隆过滤，周期重建实现批量重抓；
2. 首页、热点、核心帖子、商品：存入KV做精准标记，想重抓直接手动修改KV状态，完全不受布隆限制；
3. 布隆只过滤海量低频页面，KV管控少量高频刷新页面。

### 优缺点
✅ 兼顾内存占用 + 灵活定点重抓，性能与可控性平衡最好；
❌ 架构略微复杂。

## 方案4：过期时间布隆（时间轮布隆，轻量化定时刷新）
给每一组bit绑定过期时间，URL标记后自动倒计时，到期自动失效，到期后自动允许重抓。
适合固定间隔统一刷新的场景。

# 三、整理面试标准答案
## 问题1：重抓是什么？
网页内容会持续更新，爬虫需要对已经抓取过的URL二次、多次抓取，校验内容更新，该行为就是重抓。

## 问题2：普通布隆为什么无法满足重抓？
标准布隆过滤器仅支持插入，不支持删除；多个URL哈希共享bit位，删除单个元素会污染其他URL标记，因此无法单独放开某URL的抓取限制。

## 问题3：3种标准解决思路
1. **周期重建**：定期废弃旧布隆、新建空过滤器，全局统一放开重抓，实现最简单；
2. **计数布隆过滤器**：bit改为计数器，支持单独删除标记，定点随时重抓，内存开销提升；
3. **分层架构**：海量URL用布隆粗略去重，高频重点URL用KV精确管理，灵活手动控制重抓。

# 补充：前文提到的布隆误判（假阳性）回顾
假阳性：过滤器判定URL已抓取，实际从未抓取，造成漏抓。
解决：
1. 高价值页面落地KV精确去重；
2. 合理调高哈希函数数量，降低误判概率；
3. 接受极少量漏抓，互联网爬虫可以容忍轻微漏抓。
```




#### 深入:不止 URL 去重,还有内容去重(SimHash 近重复)⭐⭐⭐(原书没讲,区分候选人的点)

URL 去重(布隆)只解决"同一 URL 不重复抓"。但爬虫更大的坑是:**不同 URL 指向高度相似的内容**——镜像站、转载、聚合页。10 亿页里 30%+ 是近重复。这才是搜索引擎真正的难题,也是面试里区分"背过书"和"真做过"的点。

| 去重类型 | 方法 | 能否检测"相似"(非完全相同) |
|---------|------|---------------------------|
| URL 去重 | 布隆过滤器 | ❌ 只判完全相同 |
| 内容精确去重 | 内容 MD5/SHA | ❌ 改一个字就不同 |
| **内容近重复** | **SimHash / MinHash** | **✅ 检测 90%+ 相似** |

**SimHash 流程**(Google 2007 论文,工业标配):

```mermaid
flowchart LR
    HTML["网页内容"] --> FEAT["特征提取<br/>(分词/去停用词)"]
    FEAT --> HASH["每词哈希成 64 位<br/>加权累加"]
    HASH --> SIGN["符号化<br/>(正→1 负→0)"]
    SIGN --> FP["64 位指纹"]
    FP --> CMP{"海明距离 ≤ 3?"}
    CMP -->|"是"| DUP["判为近重复<br/>只保留一份"]
    CMP -->|"否"| NEW["新内容 → 入库"]

    style HTML fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style FEAT fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style HASH fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style SIGN fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style FP fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style CMP fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style DUP fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style NEW fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
```

**关键点**:
- SimHash 把整篇文档压成 **64 位指纹**;局部改动(改几个词)只翻转少数几位。
- 两个文档**海明距离 ≤ 3** → 判为近重复(经验阈值)。
- **快速检索近重复**:把 64 位**分 4 段建倒排索引**(抽屉原理)——距离 ≤ 3 的两指纹必有一段完全相同 → 按段召回候选,再精确算海明距离。否则两两比对是 O(n²),10 亿页算不完。

> 💡 **面试加分**:"URL 去重只是第一步。生产爬虫还要做**内容近重复检测(SimHash)**,否则索引库里全是镜像/转载,搜出 10 条一样的结果。这是 Google 2007 论文的经典方法。"

> 🪤 **追问陷阱**:"10 亿文档怎么快速找近重复?" → "SimHash + **分段倒排(抽屉法)**。64 位分 4 段,距离 ≤ 3 的两指纹必有一段相同,按段建索引召回,再精确算海明距离。把 O(n²) 降到可接受。"

---

## 5. DNS 缓存(性能瓶颈)⭐

**问题**:每次下载都要 DNS 解析,而 **DNS 解析很慢(10-200ms)**,是爬虫的隐藏瓶颈。

```mermaid
flowchart LR
    DQ["下载器"] --> Q{"DNS 缓存有?"}
    Q -->|"是"| HIT["直接用 IP(快)"]
    Q -->|"否"| RES["DNS 解析<br/>(慢, 10-200ms)"]
    RES --> CACHE["存缓存"]
    CACHE --> HIT

    style DQ fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style Q fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style HIT fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style RES fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style CACHE fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

**解法**:**本地 DNS 缓存**(维护 IP → 域名映射),命中直接用,不每次查 DNS。Google 爬虫有专门的 DNS 缓存层。

---

## 6. politeness(robots.txt + 延迟)

```mermaid
flowchart LR
    SITE["站点"] --> RB{"robots.txt<br/>允许?"}
    RB -->|"禁止"| SKIP["跳过 ❌"]
    RB -->|"允许"| GRAB["抓取"]
    GRAB --> DELAY["延迟 N 秒<br/>(politeness)"]
    DELAY --> NEXT["下一个同站 URL"]

    style SITE fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style RB fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style SKIP fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style GRAB fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style DELAY fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style NEXT fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
```

> 📝 **robots.txt**:站点根目录的协议文件,声明哪些页面**允许/禁止**爬虫抓取。**必须遵守**(法律 + 礼仪)。

> 🪤 **追问陷阱**:"不遵守 robots.txt 行不行?" → "技术上能抓,但**会被封 IP + 法律风险**。商业爬虫(Google/Bing)都遵守。"

---

## 7. 优先级 + fresh

```mermaid
flowchart LR
    FRESH["重新抓取策略"] --> Q{"页面更新频率?"}
    Q -->|"高频(新闻)"| H["每天重抓"]
    Q -->|"中频(博客)"| M["每周"]
    Q -->|"低频(静态)"| L["每月"]
    Q --> PR["优先级<br/>PageRank 高的先抓"]

    style FRESH fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style Q fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style H fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style M fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style L fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style PR fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

- **优先级**:PageRank/重要度高的页面优先抓(首页 vs 深层页)。
- **fresh**:根据页面历史更新频率,决定重抓周期。

---

## 8. 容错(健壮性)⭐

```mermaid
flowchart TD
    R["容错要处理的坑"] --> A["① 超时<br/>(慢站点)"]
    R --> B["② 黑洞<br/>(返回超大内容耗尽带宽)"]
    R --> C["③ 爬虫 trap<br/>(无限生成 URL 的动态页)"]
    R --> D["④ 循环<br/>(URL 重定向死循环)"]
    R --> E["⑤ 崩溃恢复<br/>(断点续抓)"]

    style R fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style A fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style B fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style C fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style D fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style E fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
```

| 坑 | 解法 |
|----|------|
| 超时 | 设下载超时(如 10s),超时放弃 |
| 黑洞 | 限制单页最大 size(如 10MB) |
| **爬虫 trap** | 限制单域名 URL 数 + 深度上限 |
| 重定向循环 | 限制重定向次数(如 5 次) |
| 崩溃恢复 | frontier 持久化(已抓/未抓状态存盘) |

# 【🌹自己笔记】 深入讲坑怎么处理
```
# 爬虫常见5类异常通俗详解（结合爬虫工作场景，贴合URL Frontier抓取流程）
## 一、超时（下载超时）
### 定义
向网站服务器发起网络请求建立连接、传输网页数据时，长时间没有响应、一直卡住不动，就叫**超时**。
### 产生原因
1. 目标服务器卡顿、带宽拥堵；
2. 目标网站服务器距离很远，网络链路差；
3. 对方服务器故意慢速拖慢爬虫请求，反爬防护。
### 危害
爬虫线程会一直阻塞卡死，一直等待响应，大量线程挂死，整个爬虫调度瘫痪，无法继续抓取新链接。
### 解决方案
设置**固定超时阈值**（行业常用10s）：
- 发起请求后倒计时；
- 10秒内没有返回网页内容，直接强制断开连接，丢弃当前URL，标记抓取失败，不再等待。

## 二、黑洞（无限大网页黑洞）
### 定义
部分网站存在特殊动态页面，页面没有固定结尾，会**源源不断无限加载数据**，网页体积持续膨胀，永远下载不完，这类页面就是爬虫黑洞。
### 典型例子
1. 无限分页动态页面：`shturl.cc/uSxwZY8Lq`，page可以无限递增，页面不断拼接内容，越下载越大；
2. 流式长文本、无限数据流网页。
### 危害
爬虫会一直下载这个超大页面，占用大量带宽、磁盘、内存，线程长时间占用，卡死调度队列。
### 解决方案
限制单页面最大下载体积，常用阈值10MB：
- 下载过程实时统计已接收数据大小；
- 一旦超过10MB，立刻终止下载，丢弃该页面。

## 三、爬虫 Trap（爬虫陷阱，死链陷阱）
### 定义
网站故意构造大量无价值、无限层级、闭环的虚假URL，专门困住爬虫，消耗爬虫算力、带宽，让爬虫一直在无效链接里循环，无法抓取有效内容。
### 两类最常见陷阱
1. **无限深度陷阱**
首页→一级目录→二级目录→三级目录……可以无限往下嵌套，层数无上限，爬虫不停往深层抓取，永远回不到核心页面。
2. **无限URL陷阱**
同一域名下随机拼接参数生成海量唯一无效链接：`shturl.cc/LKe66AUJTx随机字符串`，理论可以生成上亿个不同URL，全是空白无用页面。
### 危害
Frontier队列被海量垃圾URL塞满，有效高价值链接得不到抓取，爬虫资源被耗尽。
### 解决方案
双重限制：
1. **域名URL数量上限**：单个站点最多存入固定数量URL，超限不再收录新链接；
2. **抓取深度上限**：设置最大爬取层数（例如最多爬6层），超过深度的链接直接丢弃，不进入队列。

## 四、补充两个配套问题（顺带讲解）
### 1. 重定向循环
定义：A页面跳转B，B跳转C，C又跳转A，无限来回跳转。
危害：爬虫反复跳转，线程卡死。
解法：限制最多连续5次重定向，超限直接丢弃URL。

### 2. 崩溃恢复
定义：爬虫宕机、断电、程序崩溃，内存里Frontier队列全部丢失，已抓取、待抓取记录清零。
解法：Frontier持久化，把待爬队列、布隆过滤器、抓取记录定时写入磁盘，重启后读取磁盘数据恢复任务。

## 极简速记背诵版
1. 超时：网站迟迟不响应 → 设10s超时，超时放弃；
2. 黑洞：页面无限变大下载不完 → 限制单页最大10MB；
3. Trap陷阱：无限嵌套、海量垃圾链接困住爬虫 → 限域名链接总数+页面深度；
4. 重定向循环：页面来回跳转闭环 → 最多跳转5次；
5. 崩溃丢失任务：内存数据易丢 → Frontier落地持久化存储。
```

> 💡 **爬虫 trap**:有些动态站点无限生成 URL(如日历 `?day=1,2,3...`),爬虫会陷进去。解法:**限制单域名 URL 总数 + 路径深度上限**。

---

#### 深入:分布式爬虫架构 ⭐⭐(原书简化了)

```mermaid
flowchart TB
    subgraph COORD["协调层"]
        SEED["Seed"] --> MQ["URL Frontier<br/>(Kafka / Redis 队列)"]
    end
    subgraph WORKERS["下载工作节点(水平扩展)"]
        W1["Worker 1"]
        W2["Worker 2"]
        WN["Worker N"]
    end
    subgraph STORE["存储层"]
        OBJ[("对象存储<br/>(HTML)")]
        KV[("KV<br/>(URL→状态)")]
        BF["布隆过滤器<br/>(共享/分片)"]
    end
    MQ --> W1 & W2 & WN
    W1 --> STORE
    W2 --> STORE
    WN --> STORE
    W1 -.->|"新 URL"| MQ

    style COORD fill:#E3F2FD,stroke:#1976D2,color:#1f1f1f
    style WORKERS fill:#E8F5E9,stroke:#388E3C,color:#1f1f1f
    style STORE fill:#F3E5F5,stroke:#7B1FA2,color:#1f1f1f
    style SEED fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style MQ fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style W1 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style W2 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style WN fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style OBJ fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style KV fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style BF fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

**关键**:Worker 无状态,水平扩展;Frontier 用 Kafka/Redis 共享;布隆过滤器**分片**(每个 worker 一份,或集中服务)。这是 Google/Mercator 的架构。

---

#### 深入:2026 反爬——JS 渲染(原书完全没讲)⭐⭐⭐

```mermaid
flowchart LR
    MODERN["2026 网页挑战"] --> A["① JS 渲染<br/>(SPA, HTTP 抓不到内容)"]
    MODERN --> B["② 验证码<br/>(reCAPTCHA)"]
    MODERN --> C["③ IP 封禁<br/>(频繁抓被封)"]
    MODERN --> D["④ 行为检测<br/>(Cloudflare bot 防护)"]

    style MODERN fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style A fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style B fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style C fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style D fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

| 挑战 | 2026 解法 |
|------|----------|
| **JS 渲染**(React/Vue SPA) | **Headless 浏览器**(Playwright / Puppeteer)渲染后抓 DOM |
| 验证码 | 第三方打码服务 / ML 识别(谨慎,合规边界) |
| IP 封禁 | **代理池 + IP 轮换** |
| Bot 检测(Cloudflare) | 模拟真人(浏览器指纹、随机延迟) |

> 🔄 **2026 现代版话术**(原书只讲 HTTP 抓取,已严重过时):
> "2026 大量网页是 JS 渲染的 SPA,传统 HTTP GET 抓不到内容(拿到的是空壳 HTML)。现代爬虫必须区分:**静态页用 HTTP(快)**,**JS 渲染页用 Headless 浏览器(Playwright,慢但能渲染)**。再用代理池 + 轮换 IP 防封。这是原书 2020 版没覆盖的核心挑战。"

> 🪤 **追问陷阱**:"JS 渲染很慢,怎么平衡?" → "① **优先 HTTP 抓取**(大部分页是静态);② JS 页才用 Headless;③ Headless 用**无头 Chrome 池**(复用,避免每次启动);④ 并发受限(Headless 重,单机几十并发)。"

---

## 💻 代码示例(简化爬虫核心)

```python
import requests
from bs4 import BeautifulSoup

seen = set()   # 实际用布隆过滤器
frontier = PriorityQueue()

def crawl():
    while url := frontier.pop():
        host = hostname(url)
        if not robots_allows(url): continue
        sleep_until_polite(host)              # politeness 延迟
        try:
            resp = requests.get(url, timeout=10)  # 超时
            if len(resp.content) > 10*1024*1024: continue  # 防黑洞
        except Timeout:
            continue
        html = resp.text
        store(url, html)
        for new_url in extract_links(html):
            if not bloom_check(new_url):       # 布隆去重
                bloom_add(new_url)
                frontier.push(new_url, priority=estimate(url))
```

---

## ⚠️ 已过时 / 书里没说(2020 → 2026)

| 原书 | 2026 现状 | 面试应对 |
|------|----------|---------|
| 只讲 HTTP 抓取 | **JS 渲染页**必须 Headless 浏览器 | 主动提 Playwright + 何时用 |
| 反爬几乎没提 | **IP 轮换 + 代理池 + bot 检测** 是核心 | 讲反爬策略 |
| 布隆过滤器一笔带过 | 要会讲**假阳性 + 内存优势** | 给 1GB vs 100GB 对比 |
| DNS 缓存只提一句 | 是**隐藏性能瓶颈** | 强调 DNS 缓存层 |
| 没提断点续抓 | frontier **持久化**才能恢复 | 讲崩溃恢复 |
| 没提 sitemap | 大站用 **sitemap.xml** 加速发现 | 提 sitemap 补充 seed |
| **只讲 URL 去重** | **内容近重复(SimHash)** 才是真难题(30%+ 是镜像/转载) | 讲 SimHash + 抽屉法检索 |

---

## 🪤 追问陷阱

1. **"怎么避免重复抓?"** → 布隆过滤器(URL 哈希)+ 重要页面精确 KV 兜底。
2. **"同站点被反爬封怎么办?"** → politeness 延迟 + 代理池 IP 轮换。
3. **"JS 渲染页怎么抓?"** → Headless 浏览器(Playwright)。
4. **"爬虫 trap 怎么防?"** → 限制单域名 URL 数 + 路径深度上限。
5. **"DNS 慢怎么办?"** → 本地 DNS 缓存(命中直接用 IP)。
6. **"10 亿 URL 怎么去重?"** → 布隆过滤器(1-2GB)+ 分片。
7. **"新内容怎么发现?"** → sitemap.xml + ping 通知 + 重抓高频站点。
8. **"爬虫 worker 怎么扩展?"** → 无状态 worker + Kafka frontier 水平扩展。
9. **"镜像站/转载怎么去重?"** → URL 去重不够,要 **SimHash 内容指纹** + 海明距离 ≤ 3 判近重复(抽屉法快速检索)。

---

## 🏭 生产级产品速查表

| 产品 | 特点 |
|------|------|
| **Googlebot** | 全球最大,分布式,JS 渲染(WRS) |
| **Bingbot** | 类似 Google |
| **Scrapy** | Python 开源爬虫框架 |
| **Mercator / Heritrix** | 学术/归档爬虫 |
| **Playwright / Puppeteer** | Headless 浏览器(JS 渲染) |
| **Apache Nutch** | 分布式开源爬虫 |

---

## 📝 本章要点总结

```mermaid
flowchart LR
    ROOT(["Ch9 网络爬虫<br/>取-下-解-存-去重-入队"])

    B1["需求+估算<br/>────────<br/>• 10亿页/月<br/>• ~410 QPS<br/>• 500PB 存储"]
    B2["BFS+frontier ⭐<br/>────────<br/>• BFS 不 DFS<br/>• 优先级队列<br/>• politeness 队列(同站延迟)"]
    B3["去重 ⭐<br/>────────<br/>• URL去重:布隆过滤器(1GB)<br/>• 内容去重:SimHash(原书没讲)<br/>• 假阳性可接受<br/>• 重抓用 Counting Bloom"]
    B4["性能<br/>────────<br/>• DNS 缓存(隐藏瓶颈)<br/>• robots.txt 礼仪<br/>• 优先级+fresh"]
    B5["容错<br/>────────<br/>• 超时/黑洞/trap/循环<br/>• frontier 持久化"]
    B6["2026反爬 ⭐<br/>────────<br/>• JS渲染→Headless(Playwright)<br/>• IP轮换+代理池<br/>• bot检测(Cloudflare)"]

    ROOT --> B1
    ROOT --> B2
    ROOT --> B3
    ROOT --> B4
    ROOT --> B5
    ROOT --> B6

    style ROOT fill:#FFE082,stroke:#F57C00,color:#1f1f1f,stroke-width:3px
    style B1 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style B2 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style B3 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style B4 fill:#90CAF9,stroke:#1976D2,color:#1f1f1f
    style B5 fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style B6 fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
```

**核心 Takeaways**:

1. **核心循环**:取→下→解→存→去重→入队,这是爬虫心脏。
2. **BFS + 双队列 frontier**:优先级(重要页先)+ politeness(同站延迟)。
3. **去重两层**:URL 去重(布隆,省内存 100 倍)+ **内容近重复去重(SimHash)**——后者原书没讲,是区分候选人的点。
4. **DNS 缓存**是隐藏性能瓶颈,必须有。
5. **容错五坑**:超时/黑洞/trap/循环/崩溃,各有解法。
6. **2026 反爬是核心增量**:JS 渲染用 Headless(Playwright),原书完全没讲——这是你和别人的区分点。
7. **分布式**:无状态 worker + Kafka frontier 水平扩展。

> 🔗 **连接下一章**:Ch9 爬虫是"主动抓数据";**Ch10 通知系统**是"主动推数据给用户"——SMS/Email/Push,另一个高频设计题。

---

## ☁️ AWS 实现(Day 0 → 扩展 → Day N)⭐⭐⭐⭐⭐

> **本节定位**:这是 **AWS 书 Part III / Ch15 的落地视角**——把前面 SDE 设计的分布式爬虫(URL frontier + 布隆去重 + worker 池 + 内容存储)真正搬上 AWS。核心思路:**前面讲的每个抽象组件,在 AWS 都有对应的托管服务**,不必自建。引用 Part II:`aws_11`(EC2/ECS/Lambda 计算)、`aws_12`(SQS/Kinesis/Step Functions 编排)、`aws_10`(DynamoDB/S3/OpenSearch 存储)。

> 💡 **一句话开场(背)**:"前面的爬虫设计是**逻辑架构**(frontier/去重/存储);本节是**物理实现**——URL 队列用 **SQS**,worker 用 **ECS Fargate/EC2**,去重表用 **DynamoDB**,原始页面存 **S3**,全文索引用 **OpenSearch**。Day 0 先用托管服务跑通,扩展期按队列深度 autoscaling,Day N 多 Region 抓取 + S3+Athena 离线分析。"

---

### 🗺️ AWS 架构总览

把 SDE 设计的爬虫核心循环映射到 AWS 服务,每个抽象组件都对应一个托管服务,这样**不必自建基础设施**:

```mermaid
flowchart LR
    SEED["Seed URLs<br/>(DynamoDB)"] --> SQS["URL 待爬队列<br/>(SQS / SQS FIFO)"]
    SQS --> ECS["Crawler Worker<br/>(ECS Fargate / EC2)"]
    ECS -->|"抓取+解析"| WEB["互联网<br/>(NAT Gateway)"]
    WEB --> ECS
    ECS --> DEDUP{"去重?<br/>(DynamoDB)"}
    DEDUP -->|"新 URL"| SQS
    DEDUP -->|"已抓"| SKIP["丢弃"]
    ECS -->|"存原始页"| S3[("S3<br/>HTML 对象存储")]
    ECS -->|"元数据"| DDB[("DynamoDB<br/>URL→S3 路径")]
    S3 --> OS["OpenSearch<br/>全文索引"]

    style SEED fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style SQS fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style ECS fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style WEB fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style DEDUP fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style SKIP fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style S3 fill:#90CAF9,stroke:#1976D2,color:#1f1f1f
    style DDB fill:#90CAF9,stroke:#1976D2,color:#1f1f1f
    style OS fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
```

**组件映射表**(背):

| SDE 设计组件 | AWS 服务 | 为什么 |
|-------------|---------|--------|
| URL frontier(待爬队列) | **SQS / SQS FIFO** | 托管队列,自动削峰,FIFO 保同站点顺序(politeness) |
| Crawler worker(下载器) | **ECS Fargate / EC2** | 容器化 worker,水平扩展,Fargate 免运维 |
| 去重表(已爬 URL) | **DynamoDB** | KV 查询毫秒级,按 partition key=URL 直查 |
| 原始页面存储 | **S3** | 对象存储,极低成本,无限容量,自动多 AZ 复制 |
| 全文索引/搜索 | **OpenSearch** | 托管 Elasticsearch,倒排索引开箱即用 |
| robots/DNS 缓存 | **DynamoDB / ElastiCache** | 低延迟读,省往返 |

---

### Day 0 MVP(最小可跑通)

Day 0 目标:**最快跑通抓取循环**,全部用托管/无服务器服务,不操心扩缩容。

```mermaid
flowchart LR
    SFN["Step Functions<br/>(状态机编排)"] --> L1["Lambda: 取 URL"]
    L1 --> L2["Lambda: 抓取页面"]
    L2 --> L3["Lambda: 解析+提新 URL"]
    L3 --> L4["Lambda: 去重检查"]
    L4 -->|"新"| L5["Lambda: 存 S3 + 入队"]
    L4 -->|"已抓"| END["结束"]

    style SFN fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style L1 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style L2 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style L3 fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style L4 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style L5 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style END fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
```

**Day 0 选型**(AWS 书原话思路):
- **编排**:用 **Step Functions**(状态机)把"取 URL → 抓取 → 解析 → 去重 → 入队"拆成独立步骤,每步一个 Lambda,Map state 并行化。
- **URL 队列**:用 **SQS** 当待爬队列(URL frontier)。URL 列表太大无法在状态间传递,必须持久化到 SQS/DynamoDB。
- **去重表**:**DynamoDB** 两张表——① frontier 表(跨 crawl 持久化存 URL+TTL);② 单次 crawl 去重表(crawl 开始建,crawl 结束删,保证单 URL 不重复抓)。
- **原始页面**:**S3** 存 HTML(对象存储首选);DynamoDB 存 URL→S3 路径的元数据映射。
- **搜索**:Day 0 直接用 **Amazon Kendra**(托管企业搜索,S3 连接器自动索引),连爬虫都不用自己写——Kendra 自带 web crawler connector。

**关键 CLI / Task 定义**(ECS Fargate worker 跑抓取):

```yaml
# ECS Task Definition(简化)
family: web-crawler-worker
networkMode: awsvpc
requiresCompatibilities: [FARGATE]
cpu: "1024"          # 1 vCPU
memory: "2GB"
containerDefinitions:
  - name: crawler
    image: <account>.dkr.ecr.<region>.amazonaws.com/crawler:latest
    environment:
      - name: SQS_QUEUE_URL
        value: https://sqs.<region>.amazonaws.com/<acct>/url-frontier.fifo
      - name: DEDUP_TABLE
        value: crawled-urls
      - name: S3_BUCKET
        value: raw-pages-prod
    logConfiguration:
      logDriver: awslogs
```

```python
# worker 核心循环(ECS 容器内跑)
import boto3, requests, hashlib
from bs4 import BeautifulSoup

sqs, ddb, s3 = boto3.client("sqs"), boto3.resource("dynamodb").Table("crawled-urls"), boto3.client("s3")

def poll_and_crawl():
    while True:
        msgs = sqs.receive_message(QueueUrl=Q, MaxNumberOfMessages=10, WaitTimeSeconds=20).get("Messages", [])
        for m in msgs:
            url = m["Body"]
            key = hashlib.sha256(url.encode()).hexdigest()
            # 去重:ConditionalWrite,已存在则跳过
            try:
                ddb.put_item(Item={"url_hash": key, "url": url},
                             ConditionExpression="attribute_not_exists(url_hash)")
            except Exception:  # 已抓过
                sqs.delete_message(QueueUrl=Q, ReceiptHandle=m["ReceiptHandle"]); continue
            html = requests.get(url, timeout=10).text
            s3.put_object(Bucket="raw-pages-prod", Key=f"{key}.html", Body=html.encode())
            for new_url in BeautifulSoup(html, "html.parser").find_all("a", href=True):
                sqs.send_message(QueueUrl=Q, MessageBody=new_url["href"],
                                 MessageGroupId=domain_of(new_url["href"]))  # FIFO 按域名分组
            sqs.delete_message(QueueUrl=Q, ReceiptHandle=m["ReceiptHandle"])
```

> 📝 **Day 0 关键点**:用 **DynamoDB ConditionalWrite** 实现"检查-插入"原子操作(避免并发重复抓);SQS FIFO 的 **MessageGroupId=域名** 实现同站点串行(politeness 免费拿到)。

---

### 扩展(单 Region)

Day 0 跑通后,瓶颈出现:① Lambda 冷启动 + 超时(15 分钟上限)不适合长抓取;② 单 worker 吞吐不够。扩展期解决:

```mermaid
flowchart LR
    SQS["SQS 队列<br/>(ApproximateNumberOfMessages)"] --> CW["CloudWatch 指标"]
    CW --> ASG["ECS Service Auto Scaling<br/>(按队列深度扩 worker)"]
    ASG --> W1["Worker ×N<br/>(Fargate)"]
    W1 --> RATE{"令牌桶限速<br/>(DynamoDB)"}
    RATE --> WEB["互联网"]
    W1 -->|"解析后"| OS["OpenSearch<br/>全文索引"]
    W1 -->|"去重"| BF["Bloom Filter(内存)<br/>或 DynamoDB"]

    style SQS fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style CW fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style ASG fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style W1 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style RATE fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style WEB fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style OS fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style BF fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
```

**扩展期三件事**:

1. **Worker 用 ECS Service + Auto Scaling**:从 Lambda 换到 **ECS Fargate**(无 15 分钟超时限制,适合长抓取/JS 渲染)。按 **SQS 队列深度**(`ApproximateNumberOfMessages`)做 target tracking autoscaling——队列积压就扩 worker,空了就缩。引用 `aws_11` 的 ECS Service Auto Scaling。

2. **抓取限速(令牌桶)**:politeness 要求同站点延迟。用 **DynamoDB** 存"域名→上次抓取时间",worker 抓前查+写;或用**令牌桶**(每域名一个桶,按速率补令牌)。也可以 ElastiCache Redis 做分布式限速。

3. **去重两层**:
   - **URL 去重**:数据量大用 **Bloom Filter**(worker 内存,1-2GB 省百倍);需要精确 + 重抓支持用 **DynamoDB**(Counting 思路:存抓取次数 + TTL)。
   - **页面解析后入 OpenSearch**:解析出的标题/正文/链接入 OpenSearch 建倒排索引,支持全文检索(引用 `aws_10` 的 OpenSearch)。

---

### Day 0 → Day N 对照表

SDE 设计的抽象组件 → AWS 服务的完整映射,这张表是面试核心:

| SDE 设计组件 | Day 0(托管) | 扩展期 | Day N(生产级) |
|-------------|------------|--------|--------------|
| URL frontier | SQS | SQS FIFO(按域名分组) | SQS FIFO + SNS 优先级路由 |
| 下载 worker | Lambda + Step Functions | ECS Fargate + Auto Scaling | EKS EC2(多 Region 部署) |
| URL 去重 | DynamoDB(单次 crawl 表) | Bloom Filter(内存)+ DynamoDB | DynamoDB Global Tables(多 Region) |
| robots/DNS 缓存 | DynamoDB | ElastiCache Redis | ElastiCache + 多 Region 副本 |
| 原始页存储 | S3 | S3 + 生命周期策略(转 Glacier) | S3 + Cross-Region Replication |
| 全文索引 | Kendra(托管) | OpenSearch | 自建倒排索引(S3 + Lucene) |
| 编排 | Step Functions | Step Functions / MWAA | Step Functions(批量重抓) |
| 监控 | CloudWatch | CloudWatch + X-Ray | CloudWatch + FIS(Chaos) |

> 💡 **选型权衡(背)**:Day 0 全用托管(Kendra/Lambda/Step Functions)图快;Day N 当规模到 Google 的 1/10 时,**自建比 Kendra 更省成本 + 更可控**——这正是 AWS 书强调的"管理服务 vs 自建"权衡(无免费午餐 TINSTAAFL,见 `aws_01` 准则四)。

---

### Day N 生产级

Day N 解决大规模生产环境的真实难题:多 Region、礼貌抓取、海量离线分析、监控、批量重抓。

```mermaid
flowchart TD
    MULTI["多 Region 抓取"] --> R1["美东 ECS 抓北美站"]
    MULTI --> R2["美西 ECS 抓亚太站"]
    MULTI --> R3["法兰克福 ECS 抓欧洲站"]
    R1 --> S3G["S3(全局,跨 Region 复制)"]
    R2 --> S3G
    R3 --> S3G
    S3G --> ATH["Athena<br/>(海量页面离线分析)"]
    S3G --> OS["OpenSearch 集群<br/>(全文索引)"]
    CW["CloudWatch"] --> M1["抓取成功率"]
    CW --> M2["队列积压"]
    CW --> M3["p99 抓取延迟"]
    SFN["Step Functions"] --> RECR["批量重抓编排"]

    style MULTI fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style R1 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style R2 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style R3 fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style S3G fill:#90CAF9,stroke:#1976D2,color:#1f1f1f
    style ATH fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style OS fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style CW fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
    style M1 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style M2 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style M3 fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style SFN fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style RECR fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
```

**Day N 五大要点**:

1. **多 Region 抓取**:爬虫部署在**靠近目标站点的 Region**(美东抓北美、法兰克福抓欧洲)。理由:① 降低 RTT 提吞吐;② 有些站点(如政府站)会封锁境外 IP,**必须在同 Region 抓**。AWS 书原话强调这点。

2. **礼貌抓取与 robots.txt**:遵守 robots.txt(法律 + 礼仪);同站点延迟用 DynamoDB 记"上次抓取时间"或令牌桶;robots 缓存存 ElastiCache 省往返。**不遵守会被封 IP + 法律风险**(见前面 SDE 章节)。

3. **S3 + Athena 离线分析**:海量原始页(20PB 级)存 S3 极便宜;用 **Athena**(无服务器 Presto)直接 SQL 查 S3 上的页面数据,做内容分析、链接图谱计算、近重复检测(SimHash)。不必把数据搬到计算集群——**计算向数据移动**。

4. **CloudWatch 监控**:核心指标——**抓取成功率**(HTTP 2xx 占比)、**SQS 队列积压**(ApproximateNumberOfMessages)、**p99 抓取延迟**、**被反爬封禁率**(429/403 占比)。配告警 + 错误预算(见 `aws_01` 增量 2)。

5. **Step Functions 编排批量重抓**:页面有 TTL(新闻每天重抓、静态页每月重抓)。用 Step Functions 编排:扫描 DynamoDB 过期 URL → 批量入队 → worker 重抓 → 更新 TTL。Step Functions 的错误处理 + 调试能力优于纯 SQS-SNS 编排(orchestration 优于 choreography,见 `aws_12`)。

---

### 💰 成本与性能

| 组件 | 计费模型 | 成本特性 |
|------|---------|---------|
| **Fargate** | 按 vCPU + 内存 × 秒 | 按任务计费,**适合波动型抓取**(队列空就缩到 0) |
| **Lambda** | 按请求数 × GB-秒 | 适合事件触发/轻量抓取,长任务不划算(15 分钟上限) |
| **EC2(EKS)** | 按实例小时 | 适合稳定大流量,Reserved Instance 省成本 |
| **S3** | 按 GB-月 + 请求数 | **存原始页成本极低**,Glacier 归档更省(冷数据) |
| **SQS** | 按请求数(非存储量) | **削峰利器**,百万消息也只算请求费 |
| **DynamoDB** | 按 RCU/WCU 或 on-demand | on-demand 适合不可预测负载;provisioned 适合稳态 |

> 💡 **成本优化(背)**:① **Fargate 按任务计费**适合爬虫的波动负载(白天多抓、夜间少抓,队列空缩到 0);② **S3 存原始页极便宜**(20PB 也能扛),配合生命周期策略把冷数据转 Glacier;③ **SQS 削峰**——突发 URL 发现不会压垮 worker,队列吸收波动;④ **Graviton(Arm)worker** 比 x86 性价比高 20-40%(见 2026 增量)。

---

### 🆕 2026 增量(AWS 书没讲的硬核)

AWS 书(2023)停在 Step Functions + Kendra,但 2026 爬虫在 AWS 上有这些新玩法:

```mermaid
flowchart LR
    NEW["2026 AWS 爬虫增量"] --> A["① Bedrock 抽结构化数据<br/>(LLM 解析页面)"]
    NEW --> B["② Lambda 事件触发抓取<br/>(S3 触发解析)"]
    NEW --> C["③ WAF/GuardDuty 防反爬封"]
    NEW --> D["④ Graviton worker 省成本"]
    NEW --> E["⑤ Step Functions Distributed Map<br/>(大规模并行)"]

    style NEW fill:#FFE082,stroke:#F9A825,color:#1f1f1f
    style A fill:#80DEEA,stroke:#0097A7,color:#1f1f1f
    style B fill:#A5D6A7,stroke:#388E3C,color:#1f1f1f
    style C fill:#EF9A9A,stroke:#C62828,color:#1f1f1f
    style D fill:#CE93D8,stroke:#7B1FA2,color:#1f1f1f
    style E fill:#FFCC80,stroke:#F57C00,color:#1f1f1f
```

| 2026 增量 | 说明 |
|----------|------|
| **Bedrock 抽结构化数据** | 传统解析靠正则/CSS 选择器(脆弱,页面改版就挂)。**Bedrock 调 LLM**(Claude 等)直接从 HTML 抽"标题/价格/作者/发布时间"等结构化字段,**抗页面改版**。适合价格监控、新闻聚合。 |
| **Lambda 事件触发抓取** | S3 上传新页 → 触发 Lambda 解析 → 结果写 DynamoDB/OpenSearch。**事件驱动管道**,不必轮询。轻量解析用 Lambda,重 JS 渲染还是 ECS。 |
| **AWS WAF / GuardDuty 防被反爬封** | 爬虫自身也可能被目标站防护(Cloudflare/WAF)。用 **AWS WAF + 住宅 IP 代理池**降低被封概率;**GuardDuty** 检测异常出站流量(避免 worker 被当成攻击源)。 |
| **Graviton(Arm)worker 省成本** | ECS Fargate / EC2 选 Graviton3/4 实例,**性价比比 x86 高 20-40%**,爬虫是 CPU 密集(解析/哈希),Graviton 收益明显。 |
| **Step Functions Distributed Map** | 2023 新增,**单次执行可并行数千 Lambda**(突破旧 Map state 上限)。批量重抓 10 万 URL 一步并行,不再要拆多个状态机。 |

> 🔄 **2026 话术(直接背)**:"AWS 书的爬虫停在 Step Functions + Kendra(2023)。**2026 三个新玩法**:① **Bedrock + LLM 解析**——传统正则/CSS 选择器页面一改版就挂,LLM 直接抽结构化字段抗改版;② **Lambda 事件驱动**(S3 上传触发解析)省轮询;③ **Graviton worker 省 20-40% 成本**。再加 **WAF/GuardDuty 防被反爬封**——爬虫本身也可能被目标站当攻击源封掉。"

---

> 🔗 **本节回扣**:SDE 设计的 URL frontier / 布隆去重 / worker 池 / 内容存储,在 AWS 分别是 **SQS / DynamoDB(Bloom) / ECS / S3**。Day 0 全托管(Kendra+Lambda+Step Functions)图快,Day N 规模上来后**自建倒排索引比 Kendra 更省更可控**——这正是 `aws_01` 准则四"无免费午餐"和准则五"看场景"的实战。下一章 **Ch10 通知系统**会从"主动抓数据"转到"主动推数据给用户",用到 SNS/SQS/Push。
